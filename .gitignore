.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# relocated caches (see cache/README) - do not commit model weights/downloads
cache/

# local model weights / checkpoints pulled by notebooks (e.g. Ollama, diffusers, HF hub)
# - keep small course-data files (csv/pdf/txt in NLP, NN, Notes, GEN-AI) out of this net
*.gguf
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# env files / secrets — never commit real credentials, pass them via env vars instead
.env
.env.*
*.pem
*.key
id_rsa*
*.pfx
*_rsa
*_dsa
*_ed25519
credentials.json
secrets.json
*.credentials

# logs
*.log
